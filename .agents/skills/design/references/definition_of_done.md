# Definition of Done

A slice design is done only when:

- required headings exist
- requirement mapping is explicit
- interfaces and failure behavior are concrete
- test strategy is present
- validation passes
