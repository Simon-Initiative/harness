# Definition of Done

Validation is complete only when:

- the validator has been run against the intended artifact scope
- findings are either fixed or explicitly reported
- no failing required checks remain
