# Validation

Primary command:

```bash
python3 .agents/skills/validate/scripts/validate_work_item.py <work_item_dir> --check prd
```

This is a hard gate. If validation fails, fix the PRD and rerun the validator.
