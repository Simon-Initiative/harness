# Issue Intake

Use the repository's configured issue-tracking flow when issue tracking is enabled in `harness.yml`.

## Required Inputs
- issue key, URL, or equivalent tracker identifier

## Source of Truth
- `harness.yml` indicates whether issue tracking is enabled
- `docs/ISSUE_TRACKING.md` defines the tracker, access method, required fields, and lookup workflow
