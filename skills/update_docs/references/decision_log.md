# Decision Log Rule

When a work-item doc is materially changed, add a decision entry to that file.

Preferred placement:

- append under an existing `## Decision Log` heading
- if missing, add `## Decision Log` near the end of the file
