# Definition of Done

Work-item doc maintenance is done only when:

- affected work item paths are identified explicitly
- PRD, FDD, and plan changes match implementation reality
- each materially changed doc includes a decision entry
- validation passes for every affected work item
