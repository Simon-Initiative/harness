# Validation

Primary command:

```bash
python3 ../validate/scripts/validate_work_item.py <work_item_dir> --check fdd
```

This is a hard gate. If validation fails, fix the FDD and rerun the validator.
