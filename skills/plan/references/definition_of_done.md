# Definition of Done

A plan is done only when:

- numbered phases exist
- each phase includes objective completion criteria
- testing and verification are explicit
- dependencies and parallelization are clear
- validation passes
