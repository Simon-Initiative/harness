# Plan Checklist

- use numbered phase headings
- every phase includes goal, tasks, testing tasks, definition of done, and gate
- `prd.md` and `fdd.md` both exist and are referenced
- phases are dependency-ordered
- safe parallel work is called out explicitly
- unknowns are captured as clarifications with default assumptions
- unresolved `TODO`, `TBD`, and `FIXME` markers are removed
