# Considerations

- keep scope intentionally narrow and reversible
- avoid broad refactors or speculative architecture work
- skip full test suite and hardening by default unless the user asks
- respect critical safety constraints
